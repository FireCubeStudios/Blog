// Typescript interface for a Post class

// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface Post { 
    id: number,
    title: string,
    description: string,
    createdAt: Date,
    tags: Array<string>,
    file: string
  }